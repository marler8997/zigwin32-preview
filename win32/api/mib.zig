//! NOTE: this source file is autogenerated, DO NOT MODIFY

//
// constants
//

//
// types
//
// --------------------------------------------------------
// Type: MIB_NOTIFICATION_TYPE
// TypeLayoutAttr: auto
// not generating the actual code for this type because it has an 'auto' layout (follow up on https://github.com/microsoft/win32metadata/issues/188)
pub const MIB_NOTIFICATION_TYPE = *opaque{
};
// --------------------------------------------------------
// Type: MIB_IF_ROW2
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IF_ROW2 = extern struct {
    InterfaceLuid: NET_LUID_LH,
    InterfaceIndex: u32,
    InterfaceGuid: Guid,
    Alias: ?[*]u16,
    Description: ?[*]u16,
    PhysicalAddressLength: u32,
    PhysicalAddress: ?[*]u8,
    PermanentPhysicalAddress: ?[*]u8,
    Mtu: u32,
    Type: u32,
    TunnelType: TUNNEL_TYPE,
    MediaType: NDIS_MEDIUM,
    PhysicalMediumType: NDIS_PHYSICAL_MEDIUM,
    AccessType: NET_IF_ACCESS_TYPE,
    DirectionType: NET_IF_DIRECTION_TYPE,
    InterfaceAndOperStatusFlags: _InterfaceAndOperStatusFlags_e__Struct,
    OperStatus: IF_OPER_STATUS,
    AdminStatus: NET_IF_ADMIN_STATUS,
    MediaConnectState: NET_IF_MEDIA_CONNECT_STATE,
    NetworkGuid: Guid,
    ConnectionType: NET_IF_CONNECTION_TYPE,
    TransmitLinkSpeed: u64,
    ReceiveLinkSpeed: u64,
    InOctets: u64,
    InUcastPkts: u64,
    InNUcastPkts: u64,
    InDiscards: u64,
    InErrors: u64,
    InUnknownProtos: u64,
    InUcastOctets: u64,
    InMulticastOctets: u64,
    InBroadcastOctets: u64,
    OutOctets: u64,
    OutUcastPkts: u64,
    OutNUcastPkts: u64,
    OutDiscards: u64,
    OutErrors: u64,
    OutUcastOctets: u64,
    OutMulticastOctets: u64,
    OutBroadcastOctets: u64,
    OutQLen: u64,
    // --------------------------------------------------------
    // Type: _InterfaceAndOperStatusFlags_e__Struct
    // TypeLayoutAttr: sequential
    // TODO: I think this is a struct, but not sure at this point, assuming it is for now
    pub const _InterfaceAndOperStatusFlags_e__Struct = extern struct {
        _bitfield: u8,
    };
};
// --------------------------------------------------------
// Type: MIB_IF_TABLE2
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IF_TABLE2 = extern struct {
    NumEntries: u32,
    Table: ?[*]MIB_IF_ROW2,
};
// --------------------------------------------------------
// Type: MIB_IPINTERFACE_ROW
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPINTERFACE_ROW = extern struct {
    Family: u16,
    InterfaceLuid: NET_LUID_LH,
    InterfaceIndex: u32,
    MaxReassemblySize: u32,
    InterfaceIdentifier: u64,
    MinRouterAdvertisementInterval: u32,
    MaxRouterAdvertisementInterval: u32,
    AdvertisingEnabled: u8,
    ForwardingEnabled: u8,
    WeakHostSend: u8,
    WeakHostReceive: u8,
    UseAutomaticMetric: u8,
    UseNeighborUnreachabilityDetection: u8,
    ManagedAddressConfigurationSupported: u8,
    OtherStatefulConfigurationSupported: u8,
    AdvertiseDefaultRoute: u8,
    RouterDiscoveryBehavior: NL_ROUTER_DISCOVERY_BEHAVIOR,
    DadTransmits: u32,
    BaseReachableTime: u32,
    RetransmitTime: u32,
    PathMtuDiscoveryTimeout: u32,
    LinkLocalAddressBehavior: NL_LINK_LOCAL_ADDRESS_BEHAVIOR,
    LinkLocalAddressTimeout: u32,
    ZoneIndices: ?[*]u32,
    SitePrefixLength: u32,
    Metric: u32,
    NlMtu: u32,
    Connected: u8,
    SupportsWakeUpPatterns: u8,
    SupportsNeighborDiscovery: u8,
    SupportsRouterDiscovery: u8,
    ReachableTime: u32,
    TransmitOffload: NL_INTERFACE_OFFLOAD_ROD,
    ReceiveOffload: NL_INTERFACE_OFFLOAD_ROD,
    DisableDefaultRoutes: u8,
};
// --------------------------------------------------------
// Type: MIB_IPINTERFACE_TABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPINTERFACE_TABLE = extern struct {
    NumEntries: u32,
    Table: ?[*]MIB_IPINTERFACE_ROW,
};
// --------------------------------------------------------
// Type: MIB_IFSTACK_ROW
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IFSTACK_ROW = extern struct {
    HigherLayerInterfaceIndex: u32,
    LowerLayerInterfaceIndex: u32,
};
// --------------------------------------------------------
// Type: MIB_INVERTEDIFSTACK_ROW
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_INVERTEDIFSTACK_ROW = extern struct {
    LowerLayerInterfaceIndex: u32,
    HigherLayerInterfaceIndex: u32,
};
// --------------------------------------------------------
// Type: MIB_IFSTACK_TABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IFSTACK_TABLE = extern struct {
    NumEntries: u32,
    Table: ?[*]MIB_IFSTACK_ROW,
};
// --------------------------------------------------------
// Type: MIB_INVERTEDIFSTACK_TABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_INVERTEDIFSTACK_TABLE = extern struct {
    NumEntries: u32,
    Table: ?[*]MIB_INVERTEDIFSTACK_ROW,
};
// --------------------------------------------------------
// Type: MIB_IP_NETWORK_CONNECTION_BANDWIDTH_ESTIMATES
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IP_NETWORK_CONNECTION_BANDWIDTH_ESTIMATES = extern struct {
    InboundBandwidthInformation: NL_BANDWIDTH_INFORMATION,
    OutboundBandwidthInformation: NL_BANDWIDTH_INFORMATION,
};
// --------------------------------------------------------
// Type: MIB_UNICASTIPADDRESS_ROW
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UNICASTIPADDRESS_ROW = extern struct {
    Address: SOCKADDR_INET,
    InterfaceLuid: NET_LUID_LH,
    InterfaceIndex: u32,
    PrefixOrigin: NL_PREFIX_ORIGIN,
    SuffixOrigin: NL_SUFFIX_ORIGIN,
    ValidLifetime: u32,
    PreferredLifetime: u32,
    OnLinkPrefixLength: u8,
    SkipAsSource: u8,
    DadState: NL_DAD_STATE,
    ScopeId: SCOPE_ID,
    CreationTimeStamp: LARGE_INTEGER,
};
// --------------------------------------------------------
// Type: MIB_UNICASTIPADDRESS_TABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UNICASTIPADDRESS_TABLE = extern struct {
    NumEntries: u32,
    Table: ?[*]MIB_UNICASTIPADDRESS_ROW,
};
// --------------------------------------------------------
// Type: MIB_ANYCASTIPADDRESS_ROW
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_ANYCASTIPADDRESS_ROW = extern struct {
    Address: SOCKADDR_INET,
    InterfaceLuid: NET_LUID_LH,
    InterfaceIndex: u32,
    ScopeId: SCOPE_ID,
};
// --------------------------------------------------------
// Type: MIB_ANYCASTIPADDRESS_TABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_ANYCASTIPADDRESS_TABLE = extern struct {
    NumEntries: u32,
    Table: ?[*]MIB_ANYCASTIPADDRESS_ROW,
};
// --------------------------------------------------------
// Type: MIB_MULTICASTIPADDRESS_ROW
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_MULTICASTIPADDRESS_ROW = extern struct {
    Address: SOCKADDR_INET,
    InterfaceIndex: u32,
    InterfaceLuid: NET_LUID_LH,
    ScopeId: SCOPE_ID,
};
// --------------------------------------------------------
// Type: MIB_MULTICASTIPADDRESS_TABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_MULTICASTIPADDRESS_TABLE = extern struct {
    NumEntries: u32,
    Table: ?[*]MIB_MULTICASTIPADDRESS_ROW,
};
// --------------------------------------------------------
// Type: MIB_IPFORWARD_ROW2
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPFORWARD_ROW2 = extern struct {
    InterfaceLuid: NET_LUID_LH,
    InterfaceIndex: u32,
    DestinationPrefix: IP_ADDRESS_PREFIX,
    NextHop: SOCKADDR_INET,
    SitePrefixLength: u8,
    ValidLifetime: u32,
    PreferredLifetime: u32,
    Metric: u32,
    Protocol: NL_ROUTE_PROTOCOL,
    Loopback: u8,
    AutoconfigureAddress: u8,
    Publish: u8,
    Immortal: u8,
    Age: u32,
    Origin: NL_ROUTE_ORIGIN,
};
// --------------------------------------------------------
// Type: MIB_IPFORWARD_TABLE2
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPFORWARD_TABLE2 = extern struct {
    NumEntries: u32,
    Table: ?[*]MIB_IPFORWARD_ROW2,
};
// --------------------------------------------------------
// Type: MIB_IPPATH_ROW
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPPATH_ROW = extern struct {
    Source: SOCKADDR_INET,
    Destination: SOCKADDR_INET,
    InterfaceLuid: NET_LUID_LH,
    InterfaceIndex: u32,
    CurrentNextHop: SOCKADDR_INET,
    PathMtu: u32,
    RttMean: u32,
    RttDeviation: u32,
    Anonymous: _Anonymous_e__Union,
    IsReachable: u8,
    LinkTransmitSpeed: u64,
    LinkReceiveSpeed: u64,
    // --------------------------------------------------------
    // Type: _Anonymous_e__Union
    // TypeLayoutAttr: explicit
    // not generating the actual code for this type because it has an explicit layout
    pub const _Anonymous_e__Union = *opaque{
    };
};
// --------------------------------------------------------
// Type: MIB_IPPATH_TABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPPATH_TABLE = extern struct {
    NumEntries: u32,
    Table: ?[*]MIB_IPPATH_ROW,
};
// --------------------------------------------------------
// Type: MIB_IPNET_ROW2
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPNET_ROW2 = extern struct {
    Address: SOCKADDR_INET,
    InterfaceIndex: u32,
    InterfaceLuid: NET_LUID_LH,
    PhysicalAddress: ?[*]u8,
    PhysicalAddressLength: u32,
    State: NL_NEIGHBOR_STATE,
    Anonymous: _Anonymous_e__Union,
    ReachabilityTime: _ReachabilityTime_e__Union,
    // --------------------------------------------------------
    // Type: _Anonymous_e__Union
    // TypeLayoutAttr: explicit
    // not generating the actual code for this type because it has an explicit layout
    pub const _Anonymous_e__Union = *opaque{
        // --------------------------------------------------------
        // Type: _Anonymous_e__Struct
        // TypeLayoutAttr: sequential
        // TODO: I think this is a struct, but not sure at this point, assuming it is for now
        pub const _Anonymous_e__Struct = extern struct {
            _bitfield: u8,
        };
    };
    // --------------------------------------------------------
    // Type: _ReachabilityTime_e__Union
    // TypeLayoutAttr: explicit
    // not generating the actual code for this type because it has an explicit layout
    pub const _ReachabilityTime_e__Union = *opaque{
    };
};
// --------------------------------------------------------
// Type: MIB_IPNET_TABLE2
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPNET_TABLE2 = extern struct {
    NumEntries: u32,
    Table: ?[*]MIB_IPNET_ROW2,
};
// --------------------------------------------------------
// Type: MIB_OPAQUE_QUERY
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_OPAQUE_QUERY = extern struct {
    dwVarId: u32,
    rgdwVarIndex: ?[*]u32,
};
// --------------------------------------------------------
// Type: MIB_IFNUMBER
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IFNUMBER = extern struct {
    dwValue: u32,
};
// --------------------------------------------------------
// Type: MIB_IFROW
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IFROW = extern struct {
    wszName: ?[*]u16,
    dwIndex: u32,
    dwType: u32,
    dwMtu: u32,
    dwSpeed: u32,
    dwPhysAddrLen: u32,
    bPhysAddr: ?[*]u8,
    dwAdminStatus: u32,
    dwOperStatus: INTERNAL_IF_OPER_STATUS,
    dwLastChange: u32,
    dwInOctets: u32,
    dwInUcastPkts: u32,
    dwInNUcastPkts: u32,
    dwInDiscards: u32,
    dwInErrors: u32,
    dwInUnknownProtos: u32,
    dwOutOctets: u32,
    dwOutUcastPkts: u32,
    dwOutNUcastPkts: u32,
    dwOutDiscards: u32,
    dwOutErrors: u32,
    dwOutQLen: u32,
    dwDescrLen: u32,
    bDescr: ?[*]u8,
};
// --------------------------------------------------------
// Type: MIB_IFTABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IFTABLE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_IFROW,
};
// --------------------------------------------------------
// Type: MIB_IPADDRROW_XP
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPADDRROW_XP = extern struct {
    dwAddr: u32,
    dwIndex: u32,
    dwMask: u32,
    dwBCastAddr: u32,
    dwReasmSize: u32,
    unused1: u16,
    wType: u16,
};
// --------------------------------------------------------
// Type: MIB_IPADDRROW_W2K
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPADDRROW_W2K = extern struct {
    dwAddr: u32,
    dwIndex: u32,
    dwMask: u32,
    dwBCastAddr: u32,
    dwReasmSize: u32,
    unused1: u16,
    unused2: u16,
};
// --------------------------------------------------------
// Type: MIB_IPADDRTABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPADDRTABLE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_IPADDRROW_XP,
};
// --------------------------------------------------------
// Type: MIB_IPFORWARDNUMBER
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPFORWARDNUMBER = extern struct {
    dwValue: u32,
};
// --------------------------------------------------------
// Type: MIB_IPFORWARDROW
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPFORWARDROW = extern struct {
    dwForwardDest: u32,
    dwForwardMask: u32,
    dwForwardPolicy: u32,
    dwForwardNextHop: u32,
    dwForwardIfIndex: u32,
    Anonymous1: _Anonymous1_e__Union,
    Anonymous2: _Anonymous2_e__Union,
    dwForwardAge: u32,
    dwForwardNextHopAS: u32,
    dwForwardMetric1: u32,
    dwForwardMetric2: u32,
    dwForwardMetric3: u32,
    dwForwardMetric4: u32,
    dwForwardMetric5: u32,
    // --------------------------------------------------------
    // Type: _Anonymous1_e__Union
    // TypeLayoutAttr: explicit
    // not generating the actual code for this type because it has an explicit layout
    pub const _Anonymous1_e__Union = *opaque{
    };
    // --------------------------------------------------------
    // Type: _Anonymous2_e__Union
    // TypeLayoutAttr: explicit
    // not generating the actual code for this type because it has an explicit layout
    pub const _Anonymous2_e__Union = *opaque{
    };
};
// --------------------------------------------------------
// Type: MIB_IPFORWARDTABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPFORWARDTABLE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_IPFORWARDROW,
};
// --------------------------------------------------------
// Type: MIB_IPNETROW_LH
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPNETROW_LH = extern struct {
    dwIndex: u32,
    dwPhysAddrLen: u32,
    bPhysAddr: ?[*]u8,
    dwAddr: u32,
    Anonymous: _Anonymous_e__Union,
    // --------------------------------------------------------
    // Type: _Anonymous_e__Union
    // TypeLayoutAttr: explicit
    // not generating the actual code for this type because it has an explicit layout
    pub const _Anonymous_e__Union = *opaque{
    };
};
// --------------------------------------------------------
// Type: MIB_IPNETROW_W2K
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPNETROW_W2K = extern struct {
    dwIndex: u32,
    dwPhysAddrLen: u32,
    bPhysAddr: ?[*]u8,
    dwAddr: u32,
    dwType: u32,
};
// --------------------------------------------------------
// Type: MIB_IPNETTABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPNETTABLE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_IPNETROW_LH,
};
// --------------------------------------------------------
// Type: MIB_IPSTATS_LH
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPSTATS_LH = extern struct {
    Anonymous: _Anonymous_e__Union,
    dwDefaultTTL: u32,
    dwInReceives: u32,
    dwInHdrErrors: u32,
    dwInAddrErrors: u32,
    dwForwDatagrams: u32,
    dwInUnknownProtos: u32,
    dwInDiscards: u32,
    dwInDelivers: u32,
    dwOutRequests: u32,
    dwRoutingDiscards: u32,
    dwOutDiscards: u32,
    dwOutNoRoutes: u32,
    dwReasmTimeout: u32,
    dwReasmReqds: u32,
    dwReasmOks: u32,
    dwReasmFails: u32,
    dwFragOks: u32,
    dwFragFails: u32,
    dwFragCreates: u32,
    dwNumIf: u32,
    dwNumAddr: u32,
    dwNumRoutes: u32,
    // --------------------------------------------------------
    // Type: _Anonymous_e__Union
    // TypeLayoutAttr: explicit
    // not generating the actual code for this type because it has an explicit layout
    pub const _Anonymous_e__Union = *opaque{
    };
};
// --------------------------------------------------------
// Type: MIB_IPSTATS_W2K
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPSTATS_W2K = extern struct {
    dwForwarding: u32,
    dwDefaultTTL: u32,
    dwInReceives: u32,
    dwInHdrErrors: u32,
    dwInAddrErrors: u32,
    dwForwDatagrams: u32,
    dwInUnknownProtos: u32,
    dwInDiscards: u32,
    dwInDelivers: u32,
    dwOutRequests: u32,
    dwRoutingDiscards: u32,
    dwOutDiscards: u32,
    dwOutNoRoutes: u32,
    dwReasmTimeout: u32,
    dwReasmReqds: u32,
    dwReasmOks: u32,
    dwReasmFails: u32,
    dwFragOks: u32,
    dwFragFails: u32,
    dwFragCreates: u32,
    dwNumIf: u32,
    dwNumAddr: u32,
    dwNumRoutes: u32,
};
// --------------------------------------------------------
// Type: MIBICMPSTATS
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIBICMPSTATS = extern struct {
    dwMsgs: u32,
    dwErrors: u32,
    dwDestUnreachs: u32,
    dwTimeExcds: u32,
    dwParmProbs: u32,
    dwSrcQuenchs: u32,
    dwRedirects: u32,
    dwEchos: u32,
    dwEchoReps: u32,
    dwTimestamps: u32,
    dwTimestampReps: u32,
    dwAddrMasks: u32,
    dwAddrMaskReps: u32,
};
// --------------------------------------------------------
// Type: MIBICMPINFO
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIBICMPINFO = extern struct {
    icmpInStats: MIBICMPSTATS,
    icmpOutStats: MIBICMPSTATS,
};
// --------------------------------------------------------
// Type: MIB_ICMP
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_ICMP = extern struct {
    stats: MIBICMPINFO,
};
// --------------------------------------------------------
// Type: MIBICMPSTATS_EX_XPSP1
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIBICMPSTATS_EX_XPSP1 = extern struct {
    dwMsgs: u32,
    dwErrors: u32,
    rgdwTypeCount: ?[*]u32,
};
// --------------------------------------------------------
// Type: MIB_ICMP_EX_XPSP1
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_ICMP_EX_XPSP1 = extern struct {
    icmpInStats: MIBICMPSTATS_EX_XPSP1,
    icmpOutStats: MIBICMPSTATS_EX_XPSP1,
};
// --------------------------------------------------------
// Type: ICMP6_TYPE
// TypeLayoutAttr: auto
// not generating the actual code for this type because it has an 'auto' layout (follow up on https://github.com/microsoft/win32metadata/issues/188)
pub const ICMP6_TYPE = *opaque{
};
// --------------------------------------------------------
// Type: ICMP4_TYPE
// TypeLayoutAttr: auto
// not generating the actual code for this type because it has an 'auto' layout (follow up on https://github.com/microsoft/win32metadata/issues/188)
pub const ICMP4_TYPE = *opaque{
};
// --------------------------------------------------------
// Type: MIB_IPMCAST_OIF_XP
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPMCAST_OIF_XP = extern struct {
    dwOutIfIndex: u32,
    dwNextHopAddr: u32,
    dwReserved: u32,
    dwReserved1: u32,
};
// --------------------------------------------------------
// Type: MIB_IPMCAST_OIF_W2K
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPMCAST_OIF_W2K = extern struct {
    dwOutIfIndex: u32,
    dwNextHopAddr: u32,
    pvReserved: ?*opaque{},
    dwReserved: u32,
};
// --------------------------------------------------------
// Type: MIB_IPMCAST_MFE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPMCAST_MFE = extern struct {
    dwGroup: u32,
    dwSource: u32,
    dwSrcMask: u32,
    dwUpStrmNgbr: u32,
    dwInIfIndex: u32,
    dwInIfProtocol: u32,
    dwRouteProtocol: u32,
    dwRouteNetwork: u32,
    dwRouteMask: u32,
    ulUpTime: u32,
    ulExpiryTime: u32,
    ulTimeOut: u32,
    ulNumOutIf: u32,
    fFlags: u32,
    dwReserved: u32,
    rgmioOutInfo: ?[*]MIB_IPMCAST_OIF_XP,
};
// --------------------------------------------------------
// Type: MIB_MFE_TABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_MFE_TABLE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_IPMCAST_MFE,
};
// --------------------------------------------------------
// Type: MIB_IPMCAST_OIF_STATS_LH
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPMCAST_OIF_STATS_LH = extern struct {
    dwOutIfIndex: u32,
    dwNextHopAddr: u32,
    dwDialContext: u32,
    ulTtlTooLow: u32,
    ulFragNeeded: u32,
    ulOutPackets: u32,
    ulOutDiscards: u32,
};
// --------------------------------------------------------
// Type: MIB_IPMCAST_OIF_STATS_W2K
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPMCAST_OIF_STATS_W2K = extern struct {
    dwOutIfIndex: u32,
    dwNextHopAddr: u32,
    pvDialContext: ?*opaque{},
    ulTtlTooLow: u32,
    ulFragNeeded: u32,
    ulOutPackets: u32,
    ulOutDiscards: u32,
};
// --------------------------------------------------------
// Type: MIB_IPMCAST_MFE_STATS
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPMCAST_MFE_STATS = extern struct {
    dwGroup: u32,
    dwSource: u32,
    dwSrcMask: u32,
    dwUpStrmNgbr: u32,
    dwInIfIndex: u32,
    dwInIfProtocol: u32,
    dwRouteProtocol: u32,
    dwRouteNetwork: u32,
    dwRouteMask: u32,
    ulUpTime: u32,
    ulExpiryTime: u32,
    ulNumOutIf: u32,
    ulInPkts: u32,
    ulInOctets: u32,
    ulPktsDifferentIf: u32,
    ulQueueOverflow: u32,
    rgmiosOutStats: ?[*]MIB_IPMCAST_OIF_STATS_LH,
};
// --------------------------------------------------------
// Type: MIB_MFE_STATS_TABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_MFE_STATS_TABLE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_IPMCAST_MFE_STATS,
};
// --------------------------------------------------------
// Type: MIB_IPMCAST_MFE_STATS_EX_XP
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPMCAST_MFE_STATS_EX_XP = extern struct {
    dwGroup: u32,
    dwSource: u32,
    dwSrcMask: u32,
    dwUpStrmNgbr: u32,
    dwInIfIndex: u32,
    dwInIfProtocol: u32,
    dwRouteProtocol: u32,
    dwRouteNetwork: u32,
    dwRouteMask: u32,
    ulUpTime: u32,
    ulExpiryTime: u32,
    ulNumOutIf: u32,
    ulInPkts: u32,
    ulInOctets: u32,
    ulPktsDifferentIf: u32,
    ulQueueOverflow: u32,
    ulUninitMfe: u32,
    ulNegativeMfe: u32,
    ulInDiscards: u32,
    ulInHdrErrors: u32,
    ulTotalOutPackets: u32,
    rgmiosOutStats: ?[*]MIB_IPMCAST_OIF_STATS_LH,
};
// --------------------------------------------------------
// Type: MIB_MFE_STATS_TABLE_EX_XP
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_MFE_STATS_TABLE_EX_XP = extern struct {
    dwNumEntries: u32,
    table: ?[*]?*MIB_IPMCAST_MFE_STATS_EX_XP,
};
// --------------------------------------------------------
// Type: MIB_IPMCAST_GLOBAL
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPMCAST_GLOBAL = extern struct {
    dwEnable: u32,
};
// --------------------------------------------------------
// Type: MIB_IPMCAST_IF_ENTRY
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPMCAST_IF_ENTRY = extern struct {
    dwIfIndex: u32,
    dwTtl: u32,
    dwProtocol: u32,
    dwRateLimit: u32,
    ulInMcastOctets: u32,
    ulOutMcastOctets: u32,
};
// --------------------------------------------------------
// Type: MIB_IPMCAST_IF_TABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPMCAST_IF_TABLE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_IPMCAST_IF_ENTRY,
};
// --------------------------------------------------------
// Type: TCP_CONNECTION_OFFLOAD_STATE
// TypeLayoutAttr: auto
// not generating the actual code for this type because it has an 'auto' layout (follow up on https://github.com/microsoft/win32metadata/issues/188)
pub const TCP_CONNECTION_OFFLOAD_STATE = *opaque{
};
// --------------------------------------------------------
// Type: MIB_TCPROW_LH
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCPROW_LH = extern struct {
    Anonymous: _Anonymous_e__Union,
    dwLocalAddr: u32,
    dwLocalPort: u32,
    dwRemoteAddr: u32,
    dwRemotePort: u32,
    // --------------------------------------------------------
    // Type: _Anonymous_e__Union
    // TypeLayoutAttr: explicit
    // not generating the actual code for this type because it has an explicit layout
    pub const _Anonymous_e__Union = *opaque{
    };
};
// --------------------------------------------------------
// Type: MIB_TCPROW_W2K
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCPROW_W2K = extern struct {
    dwState: u32,
    dwLocalAddr: u32,
    dwLocalPort: u32,
    dwRemoteAddr: u32,
    dwRemotePort: u32,
};
// --------------------------------------------------------
// Type: MIB_TCPTABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCPTABLE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_TCPROW_LH,
};
// --------------------------------------------------------
// Type: MIB_TCPROW2
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCPROW2 = extern struct {
    dwState: u32,
    dwLocalAddr: u32,
    dwLocalPort: u32,
    dwRemoteAddr: u32,
    dwRemotePort: u32,
    dwOwningPid: u32,
    dwOffloadState: TCP_CONNECTION_OFFLOAD_STATE,
};
// --------------------------------------------------------
// Type: MIB_TCPTABLE2
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCPTABLE2 = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_TCPROW2,
};
// --------------------------------------------------------
// Type: MIB_TCPROW_OWNER_PID
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCPROW_OWNER_PID = extern struct {
    dwState: u32,
    dwLocalAddr: u32,
    dwLocalPort: u32,
    dwRemoteAddr: u32,
    dwRemotePort: u32,
    dwOwningPid: u32,
};
// --------------------------------------------------------
// Type: MIB_TCPTABLE_OWNER_PID
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCPTABLE_OWNER_PID = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_TCPROW_OWNER_PID,
};
// --------------------------------------------------------
// Type: MIB_TCPROW_OWNER_MODULE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCPROW_OWNER_MODULE = extern struct {
    dwState: u32,
    dwLocalAddr: u32,
    dwLocalPort: u32,
    dwRemoteAddr: u32,
    dwRemotePort: u32,
    dwOwningPid: u32,
    liCreateTimestamp: LARGE_INTEGER,
    OwningModuleInfo: ?[*]u64,
};
// --------------------------------------------------------
// Type: MIB_TCPTABLE_OWNER_MODULE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCPTABLE_OWNER_MODULE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_TCPROW_OWNER_MODULE,
};
// --------------------------------------------------------
// Type: MIB_TCP6ROW
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCP6ROW = extern struct {
    State: MIB_TCP_STATE,
    LocalAddr: in6_addr,
    dwLocalScopeId: u32,
    dwLocalPort: u32,
    RemoteAddr: in6_addr,
    dwRemoteScopeId: u32,
    dwRemotePort: u32,
};
// --------------------------------------------------------
// Type: MIB_TCP6TABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCP6TABLE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_TCP6ROW,
};
// --------------------------------------------------------
// Type: MIB_TCP6ROW2
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCP6ROW2 = extern struct {
    LocalAddr: in6_addr,
    dwLocalScopeId: u32,
    dwLocalPort: u32,
    RemoteAddr: in6_addr,
    dwRemoteScopeId: u32,
    dwRemotePort: u32,
    State: MIB_TCP_STATE,
    dwOwningPid: u32,
    dwOffloadState: TCP_CONNECTION_OFFLOAD_STATE,
};
// --------------------------------------------------------
// Type: MIB_TCP6TABLE2
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCP6TABLE2 = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_TCP6ROW2,
};
// --------------------------------------------------------
// Type: MIB_TCP6ROW_OWNER_PID
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCP6ROW_OWNER_PID = extern struct {
    ucLocalAddr: ?[*]u8,
    dwLocalScopeId: u32,
    dwLocalPort: u32,
    ucRemoteAddr: ?[*]u8,
    dwRemoteScopeId: u32,
    dwRemotePort: u32,
    dwState: u32,
    dwOwningPid: u32,
};
// --------------------------------------------------------
// Type: MIB_TCP6TABLE_OWNER_PID
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCP6TABLE_OWNER_PID = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_TCP6ROW_OWNER_PID,
};
// --------------------------------------------------------
// Type: MIB_TCP6ROW_OWNER_MODULE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCP6ROW_OWNER_MODULE = extern struct {
    ucLocalAddr: ?[*]u8,
    dwLocalScopeId: u32,
    dwLocalPort: u32,
    ucRemoteAddr: ?[*]u8,
    dwRemoteScopeId: u32,
    dwRemotePort: u32,
    dwState: u32,
    dwOwningPid: u32,
    liCreateTimestamp: LARGE_INTEGER,
    OwningModuleInfo: ?[*]u64,
};
// --------------------------------------------------------
// Type: MIB_TCP6TABLE_OWNER_MODULE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCP6TABLE_OWNER_MODULE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_TCP6ROW_OWNER_MODULE,
};
// --------------------------------------------------------
// Type: MIB_TCPSTATS_LH
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCPSTATS_LH = extern struct {
    Anonymous: _Anonymous_e__Union,
    dwRtoMin: u32,
    dwRtoMax: u32,
    dwMaxConn: u32,
    dwActiveOpens: u32,
    dwPassiveOpens: u32,
    dwAttemptFails: u32,
    dwEstabResets: u32,
    dwCurrEstab: u32,
    dwInSegs: u32,
    dwOutSegs: u32,
    dwRetransSegs: u32,
    dwInErrs: u32,
    dwOutRsts: u32,
    dwNumConns: u32,
    // --------------------------------------------------------
    // Type: _Anonymous_e__Union
    // TypeLayoutAttr: explicit
    // not generating the actual code for this type because it has an explicit layout
    pub const _Anonymous_e__Union = *opaque{
    };
};
// --------------------------------------------------------
// Type: MIB_TCPSTATS_W2K
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCPSTATS_W2K = extern struct {
    dwRtoAlgorithm: u32,
    dwRtoMin: u32,
    dwRtoMax: u32,
    dwMaxConn: u32,
    dwActiveOpens: u32,
    dwPassiveOpens: u32,
    dwAttemptFails: u32,
    dwEstabResets: u32,
    dwCurrEstab: u32,
    dwInSegs: u32,
    dwOutSegs: u32,
    dwRetransSegs: u32,
    dwInErrs: u32,
    dwOutRsts: u32,
    dwNumConns: u32,
};
// --------------------------------------------------------
// Type: MIB_TCPSTATS2
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_TCPSTATS2 = extern struct {
    RtoAlgorithm: TCP_RTO_ALGORITHM,
    dwRtoMin: u32,
    dwRtoMax: u32,
    dwMaxConn: u32,
    dwActiveOpens: u32,
    dwPassiveOpens: u32,
    dwAttemptFails: u32,
    dwEstabResets: u32,
    dwCurrEstab: u32,
    dw64InSegs: u64,
    dw64OutSegs: u64,
    dwRetransSegs: u32,
    dwInErrs: u32,
    dwOutRsts: u32,
    dwNumConns: u32,
};
// --------------------------------------------------------
// Type: MIB_UDPROW
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UDPROW = extern struct {
    dwLocalAddr: u32,
    dwLocalPort: u32,
};
// --------------------------------------------------------
// Type: MIB_UDPTABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UDPTABLE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_UDPROW,
};
// --------------------------------------------------------
// Type: MIB_UDPROW_OWNER_PID
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UDPROW_OWNER_PID = extern struct {
    dwLocalAddr: u32,
    dwLocalPort: u32,
    dwOwningPid: u32,
};
// --------------------------------------------------------
// Type: MIB_UDPTABLE_OWNER_PID
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UDPTABLE_OWNER_PID = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_UDPROW_OWNER_PID,
};
// --------------------------------------------------------
// Type: MIB_UDPROW_OWNER_MODULE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UDPROW_OWNER_MODULE = extern struct {
    dwLocalAddr: u32,
    dwLocalPort: u32,
    dwOwningPid: u32,
    liCreateTimestamp: LARGE_INTEGER,
    Anonymous: _Anonymous_e__Union,
    OwningModuleInfo: ?[*]u64,
    // --------------------------------------------------------
    // Type: _Anonymous_e__Union
    // TypeLayoutAttr: explicit
    // not generating the actual code for this type because it has an explicit layout
    pub const _Anonymous_e__Union = *opaque{
        // --------------------------------------------------------
        // Type: _Anonymous_e__Struct
        // TypeLayoutAttr: sequential
        // TODO: I think this is a struct, but not sure at this point, assuming it is for now
        pub const _Anonymous_e__Struct = extern struct {
            _bitfield: i32,
        };
    };
};
// --------------------------------------------------------
// Type: MIB_UDPTABLE_OWNER_MODULE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UDPTABLE_OWNER_MODULE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_UDPROW_OWNER_MODULE,
};
// --------------------------------------------------------
// Type: MIB_UDP6ROW
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UDP6ROW = extern struct {
    dwLocalAddr: in6_addr,
    dwLocalScopeId: u32,
    dwLocalPort: u32,
};
// --------------------------------------------------------
// Type: MIB_UDP6TABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UDP6TABLE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_UDP6ROW,
};
// --------------------------------------------------------
// Type: MIB_UDP6ROW_OWNER_PID
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UDP6ROW_OWNER_PID = extern struct {
    ucLocalAddr: ?[*]u8,
    dwLocalScopeId: u32,
    dwLocalPort: u32,
    dwOwningPid: u32,
};
// --------------------------------------------------------
// Type: MIB_UDP6TABLE_OWNER_PID
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UDP6TABLE_OWNER_PID = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_UDP6ROW_OWNER_PID,
};
// --------------------------------------------------------
// Type: MIB_UDP6ROW_OWNER_MODULE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UDP6ROW_OWNER_MODULE = extern struct {
    ucLocalAddr: ?[*]u8,
    dwLocalScopeId: u32,
    dwLocalPort: u32,
    dwOwningPid: u32,
    liCreateTimestamp: LARGE_INTEGER,
    Anonymous: _Anonymous_e__Union,
    OwningModuleInfo: ?[*]u64,
    // --------------------------------------------------------
    // Type: _Anonymous_e__Union
    // TypeLayoutAttr: explicit
    // not generating the actual code for this type because it has an explicit layout
    pub const _Anonymous_e__Union = *opaque{
        // --------------------------------------------------------
        // Type: _Anonymous_e__Struct
        // TypeLayoutAttr: sequential
        // TODO: I think this is a struct, but not sure at this point, assuming it is for now
        pub const _Anonymous_e__Struct = extern struct {
            _bitfield: i32,
        };
    };
};
// --------------------------------------------------------
// Type: MIB_UDP6TABLE_OWNER_MODULE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UDP6TABLE_OWNER_MODULE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_UDP6ROW_OWNER_MODULE,
};
// --------------------------------------------------------
// Type: MIB_UDPSTATS
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UDPSTATS = extern struct {
    dwInDatagrams: u32,
    dwNoPorts: u32,
    dwInErrors: u32,
    dwOutDatagrams: u32,
    dwNumAddrs: u32,
};
// --------------------------------------------------------
// Type: MIB_UDPSTATS2
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_UDPSTATS2 = extern struct {
    dw64InDatagrams: u64,
    dwNoPorts: u32,
    dwInErrors: u32,
    dw64OutDatagrams: u64,
    dwNumAddrs: u32,
};
// --------------------------------------------------------
// Type: MIB_IPMCAST_BOUNDARY
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPMCAST_BOUNDARY = extern struct {
    dwIfIndex: u32,
    dwGroupAddress: u32,
    dwGroupMask: u32,
    dwStatus: u32,
};
// --------------------------------------------------------
// Type: MIB_IPMCAST_BOUNDARY_TABLE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPMCAST_BOUNDARY_TABLE = extern struct {
    dwNumEntries: u32,
    table: ?[*]MIB_IPMCAST_BOUNDARY,
};
// --------------------------------------------------------
// Type: MIB_BOUNDARYROW
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_BOUNDARYROW = extern struct {
    dwGroupAddress: u32,
    dwGroupMask: u32,
};
// --------------------------------------------------------
// Type: MIB_MCAST_LIMIT_ROW
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_MCAST_LIMIT_ROW = extern struct {
    dwTtl: u32,
    dwRateLimit: u32,
};
// --------------------------------------------------------
// Type: MIB_IPMCAST_SCOPE
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IPMCAST_SCOPE = extern struct {
    dwGroupAddress: u32,
    dwGroupMask: u32,
    snNameBuffer: ?[*]u16,
    dwStatus: u32,
};
// --------------------------------------------------------
// Type: MIB_BEST_IF
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_BEST_IF = extern struct {
    dwDestAddr: u32,
    dwIfIndex: u32,
};
// --------------------------------------------------------
// Type: MIB_PROXYARP
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_PROXYARP = extern struct {
    dwAddress: u32,
    dwMask: u32,
    dwIfIndex: u32,
};
// --------------------------------------------------------
// Type: MIB_IFSTATUS
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_IFSTATUS = extern struct {
    dwIfIndex: u32,
    dwAdminStatus: u32,
    dwOperationalStatus: u32,
    bMHbeatActive: BOOL,
    bMHbeatAlive: BOOL,
};
// --------------------------------------------------------
// Type: MIB_OPAQUE_INFO
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const MIB_OPAQUE_INFO = extern struct {
    dwId: u32,
    Anonymous: _Anonymous_e__Union,
    // --------------------------------------------------------
    // Type: _Anonymous_e__Union
    // TypeLayoutAttr: explicit
    // not generating the actual code for this type because it has an explicit layout
    pub const _Anonymous_e__Union = *opaque{
    };
};
// --------------------------------------------------------
// Type: NL_INTERFACE_OFFLOAD_ROD
// TypeLayoutAttr: sequential
// TODO: I think this is a struct, but not sure at this point, assuming it is for now
pub const NL_INTERFACE_OFFLOAD_ROD = extern struct {
    _bitfield: u8,
};

//
// functions
//

//
// Unicode Symbol Aliases
//
pub usingnamespace switch (@import("../zig.zig").unicode_mode) {
    .ansi => struct {
    },
    .wide => struct {
    },
    else => if (@import("builtin").is_test) struct {
    } else struct {
    },
};

//
// type imports
//
const Guid = @import("../zig.zig").Guid;
const NET_LUID_LH = @import("iphelper.zig").NET_LUID_LH;
const TUNNEL_TYPE = @import("networkdrivers.zig").TUNNEL_TYPE;
const NDIS_MEDIUM = @import("nativewifi.zig").NDIS_MEDIUM;
const NDIS_PHYSICAL_MEDIUM = @import("nativewifi.zig").NDIS_PHYSICAL_MEDIUM;
const NET_IF_ACCESS_TYPE = @import("networkdrivers.zig").NET_IF_ACCESS_TYPE;
const NET_IF_DIRECTION_TYPE = @import("networkdrivers.zig").NET_IF_DIRECTION_TYPE;
const IF_OPER_STATUS = @import("iphelper.zig").IF_OPER_STATUS;
const NET_IF_ADMIN_STATUS = @import("networkdrivers.zig").NET_IF_ADMIN_STATUS;
const NET_IF_MEDIA_CONNECT_STATE = @import("networkdrivers.zig").NET_IF_MEDIA_CONNECT_STATE;
const NET_IF_CONNECTION_TYPE = @import("networkdrivers.zig").NET_IF_CONNECTION_TYPE;
const NL_ROUTER_DISCOVERY_BEHAVIOR = @import("networkdrivers.zig").NL_ROUTER_DISCOVERY_BEHAVIOR;
const NL_LINK_LOCAL_ADDRESS_BEHAVIOR = @import("networkdrivers.zig").NL_LINK_LOCAL_ADDRESS_BEHAVIOR;
const NL_BANDWIDTH_INFORMATION = @import("iphelper.zig").NL_BANDWIDTH_INFORMATION;
const SOCKADDR_INET = @import("iphelper.zig").SOCKADDR_INET;
const NL_PREFIX_ORIGIN = @import("iphelper.zig").NL_PREFIX_ORIGIN;
const NL_SUFFIX_ORIGIN = @import("iphelper.zig").NL_SUFFIX_ORIGIN;
const NL_DAD_STATE = @import("iphelper.zig").NL_DAD_STATE;
const SCOPE_ID = @import("winsock.zig").SCOPE_ID;
const LARGE_INTEGER = @import("systemservices.zig").LARGE_INTEGER;
const IP_ADDRESS_PREFIX = @import("iphelper.zig").IP_ADDRESS_PREFIX;
const NL_ROUTE_PROTOCOL = @import("networkdrivers.zig").NL_ROUTE_PROTOCOL;
const NL_ROUTE_ORIGIN = @import("networkdrivers.zig").NL_ROUTE_ORIGIN;
const NL_NEIGHBOR_STATE = @import("networkdrivers.zig").NL_NEIGHBOR_STATE;
const INTERNAL_IF_OPER_STATUS = @import("iphelper.zig").INTERNAL_IF_OPER_STATUS;
const MIB_TCP_STATE = @import("iphelper.zig").MIB_TCP_STATE;
const in6_addr = @import("winsock.zig").in6_addr;
const TCP_RTO_ALGORITHM = @import("iphelper.zig").TCP_RTO_ALGORITHM;
const BOOL = @import("systemservices.zig").BOOL;

test "" {
    const constant_export_count = 0;
    const type_export_count = 102;
    const func_count = 0;
    const unicode_alias_count = 0;
    const type_import_count = 29;
    @setEvalBranchQuota(0
        + constant_export_count
        + type_export_count
        + func_count
        + unicode_alias_count
        + type_import_count
        + 2 // TODO: why do I need these extra 2?
    );
    @import("std").testing.refAllDecls(@This());
}
