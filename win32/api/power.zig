//! NOTE: this source file is autogenerated, DO NOT MODIFY

//
// constants
//

//
// types
//

//
// functions
//

// set_last_errror=False (can and should we use this?)
pub extern "api-ms-win-power-setting-l1-1-0" fn PowerWriteACValueIndex(
    RootPowerKey: HKEY,
    SchemeGuid: ?*Guid,
    SubGroupOfPowerSettingsGuid: ?*Guid,
    PowerSettingGuid: ?*Guid,
    AcValueIndex: u32,
) callconv(@import("std").os.windows.WINAPI) u32;

// set_last_errror=False (can and should we use this?)
pub extern "api-ms-win-power-setting-l1-1-0" fn PowerWriteDCValueIndex(
    RootPowerKey: HKEY,
    SchemeGuid: ?*Guid,
    SubGroupOfPowerSettingsGuid: ?*Guid,
    PowerSettingGuid: ?*Guid,
    DcValueIndex: u32,
) callconv(@import("std").os.windows.WINAPI) u32;

// set_last_errror=False (can and should we use this?)
pub extern "POWRPROF" fn PowerReadACValueIndex(
    RootPowerKey: HKEY,
    SchemeGuid: ?*Guid,
    SubGroupOfPowerSettingsGuid: ?*Guid,
    PowerSettingGuid: ?*Guid,
    AcValueIndex: ?*u32,
) callconv(@import("std").os.windows.WINAPI) u32;

// set_last_errror=False (can and should we use this?)
pub extern "POWRPROF" fn PowerReadDCValueIndex(
    RootPowerKey: HKEY,
    SchemeGuid: ?*Guid,
    SubGroupOfPowerSettingsGuid: ?*Guid,
    PowerSettingGuid: ?*Guid,
    DcValueIndex: ?*u32,
) callconv(@import("std").os.windows.WINAPI) u32;

// set_last_errror=False (can and should we use this?)
pub extern "POWRPROF" fn PowerReadACDefaultIndex(
    RootPowerKey: HKEY,
    SchemePersonalityGuid: ?*Guid,
    SubGroupOfPowerSettingsGuid: ?*Guid,
    PowerSettingGuid: ?*Guid,
    AcDefaultIndex: ?*u32,
) callconv(@import("std").os.windows.WINAPI) u32;

// set_last_errror=False (can and should we use this?)
pub extern "POWRPROF" fn PowerReadDCDefaultIndex(
    RootPowerKey: HKEY,
    SchemePersonalityGuid: ?*Guid,
    SubGroupOfPowerSettingsGuid: ?*Guid,
    PowerSettingGuid: ?*Guid,
    DcDefaultIndex: ?*u32,
) callconv(@import("std").os.windows.WINAPI) u32;

// set_last_errror=False (can and should we use this?)
pub extern "POWRPROF" fn PowerWriteACDefaultIndex(
    RootSystemPowerKey: HKEY,
    SchemePersonalityGuid: ?*Guid,
    SubGroupOfPowerSettingsGuid: ?*Guid,
    PowerSettingGuid: ?*Guid,
    DefaultAcIndex: u32,
) callconv(@import("std").os.windows.WINAPI) u32;

// set_last_errror=False (can and should we use this?)
pub extern "POWRPROF" fn PowerWriteDCDefaultIndex(
    RootSystemPowerKey: HKEY,
    SchemePersonalityGuid: ?*Guid,
    SubGroupOfPowerSettingsGuid: ?*Guid,
    PowerSettingGuid: ?*Guid,
    DefaultDcIndex: u32,
) callconv(@import("std").os.windows.WINAPI) u32;

//
// Unicode Symbol Aliases
//
pub usingnamespace switch (@import("../zig.zig").unicode_mode) {
    .ansi => struct {
    },
    .wide => struct {
    },
    else => if (@import("builtin").is_test) struct {
    } else struct {
    },
};

//
// type imports
//
const Guid = @import("../zig.zig").Guid;
const HKEY = @import("windowsprogramming.zig").HKEY;

test "" {
    const constant_export_count = 0;
    const type_export_count = 0;
    const func_count = 8;
    const unicode_alias_count = 0;
    const type_import_count = 2;
    @setEvalBranchQuota(0
        + constant_export_count
        + type_export_count
        + func_count
        + unicode_alias_count
        + type_import_count
        + 2 // TODO: why do I need these extra 2?
    );
    @import("std").testing.refAllDecls(@This());
}
